{
  "name": "nft-zo",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network rinkeby",
    "mint": "npx hardhat run scripts/mint.js --network rinkeby",
    "transfer": "npx hardhat run scripts/transfer.js --network rinkeby",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:static": "cp ./client/index.html ./static",
    "start": "npx hardhat node",
    "local:deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "local:mint": "npx hardhat run scripts/local/mint.local.js --network localhost",
    "local:transfer": "npx hardhat run scripts/local/transfer.local.js --network localhost"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@openzeppelin/contracts": "^4.0.0",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.1.3",
    "hardhat": "^2.2.1"
  }
}
