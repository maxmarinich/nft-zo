const baseUrl = 'https://maxmarinich.github.io/nft-zo/api';
const baseTokenUri = `${baseUrl}/items/`;
const baseContractUri = `${baseUrl}/contract`;
const totalSupplyLimit = 10;

module.exports = {
  baseUrl,
  baseTokenUri,
  baseContractUri,
  totalSupplyLimit,
};
